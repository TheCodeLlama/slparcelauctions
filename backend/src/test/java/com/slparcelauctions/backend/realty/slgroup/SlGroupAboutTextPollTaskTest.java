package com.slparcelauctions.backend.realty.slgroup;

import org.junit.jupiter.api.Disabled;
import org.junit.jupiter.api.Test;

/**
 * Sub-project F retires the ABOUT_TEXT verification path (V28 dropped the columns this
 * task depended on and tightened the verified_via CHECK to FOUNDER_TERMINAL only). The
 * task is reduced to a no-op until Task 21 deletes it entirely along with this test
 * file.
 */
@Disabled("Task 21 will delete SlGroupAboutTextPollTask and this test together")
class SlGroupAboutTextPollTaskTest {

    @Test
    void placeholderUntilTask21RemovesTheTaskEntirely() {
        // Intentionally empty.
    }
}
