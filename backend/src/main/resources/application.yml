server:
  port: 8080

spring:
  application:
    name: slpa-backend
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

cors:
  allowed-origin: http://localhost:3000
