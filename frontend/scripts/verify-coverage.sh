#!/usr/bin/env bash
# STUB — Task 27 replaces this with the real coverage check that confirms every
# src/components/ui/*.tsx (excluding barrels) has a sibling *.test.tsx file.
# Until then, this is a no-op so `npm run verify` stays runnable end-to-end.

set -uo pipefail

echo "verify:coverage — STUB (real coverage check ships in plan Task 27)"
exit 0
